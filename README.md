# flipbook-generate
Create an HTML flipbook from a PDF file

📘 Flipbook Generator Setup & Usage Guide

This tool converts a PDF magazine into an interactive HTML flipbook and automatically updates the archive (library.json).
